{
  "name": "@vektopay/angular",
  "type": "module",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "peerDependencies": {
    "@angular/core": ">=16"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "npx @biomejs/biome check src scripts",
    "typecheck": "npx tsc --noEmit --project tsconfig.json",
    "test": "node --test",
    "validate": "npm run lint && npm run typecheck && npm run test && npm run build",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@angular/core": "^18.2.0",
    "typescript": "^5.5.4",
    "@biomejs/biome": "^1.9.4",
    "@commitlint/config-conventional": "^19.8.1",
    "commitlint": "^19.8.1",
    "husky": "^9.1.7"
  }
}
